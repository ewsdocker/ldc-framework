    -it \
    -v \${HOME}/bin:/userbin \
    -v \${HOME}/.local:/usrlocal \
    -v \${HOME}/.config/docker:/conf \
    -v \${HOME}/.config/docker/${LMS_RUN_NAME}:/root \
    -v \${HOME}/.config/docker/${LMS_RUN_NAME}/workspace:/workspace \
    -v /etc/localtime:/etc/localtime:ro \
    -e LMS_HOME=\${HOME} \
    -e LMS_BASE=\${HOME}/.local \
    -e LMS_CONF=\${HOME}/.config \
